// hold component validations.

const EmsolValidate = (data, type) => {
  // individual input validation type and validation method.

  //return validation flag {true/false} error messages.
  return data;
};

export default EmsolValidate;

//  data=> the user entered data
// type=> to identify the type for validation
