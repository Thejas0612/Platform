import React from "react";
import { CustomTag } from "@emerson/dynamic-ui";
function CustomTag() {
  return (
    <>
      <CustomTag
        data={[]}
        label={{}}
        msg="It will reset the record. Are you sure you want to continute?"
        onChange={() => {}}
        showLabel
      />
    </>
  );
}

export default CustomTag;
