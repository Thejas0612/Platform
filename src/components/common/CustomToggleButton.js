import React from "react";
import {CustomToggleButton } from "@emerson/dynamic-ui"
function CustomToggleButton() {
  return (
    <>
      <CustomToggleButton data={[]} defaultId={{}} label={{}} onChange={() => {}} />
    </>
  );
}

export default CustomToggleButton;
