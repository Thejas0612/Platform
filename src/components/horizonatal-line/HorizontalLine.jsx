const HorizontalLine = (schema) => {
  return (
    <div>
        <hr
          style={{
            border: "none",
            height: "2px",
            backgroundColor: "#DEDFE0",
            color: "#DEDFE0",
            borderRadius: "1rem",
            margin: "2rem 0"
          }}
        />
    </div>
  );
};

export default HorizontalLine;
