//import axios from 'axios';
// Api call file
export const getLayoutApi = async () => {
  // application type to a dynamic value
  // const { data } = await axios.get(
  //   `${process.env.REACT_APP_API_BASE_URL}api/pilot/layout?type=dpFlow`
  // );
  return [];
};

export const getApplicationSections = async (values) => {
  // const { data } = await axios.post(
  //   `${process.env.REACT_APP_API_BASE_URL}api/pilot-get-section-details`,
  //   values
  // );
  return [];
};

export const getDynamicFields = async (values) => {
  // const { data = {} } = await axios.post(
  //   `${process.env.REACT_APP_API_BASE_URL}api/pilot/getfields`,
  //   values
  // );
  return [];
};
