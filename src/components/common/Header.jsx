import React  from "react";

const HeaderSection = () => {
  return (
    <>
      {/* <Header
        isAuthenticated
        logo={`${logo}`}
        menuData={data}
        title={`${title}`}
        userInfo={userInfo}
      /> */}
    </>
  );
};

export default HeaderSection;
